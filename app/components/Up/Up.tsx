import { Icon } from '@/app/assets/Icon';
import styles from './Up.module.css';

export const Up = () => {
    return (
        <div className={styles.up}>
            <Icon.ArrowUpIcon />
        </div>
    )
}
